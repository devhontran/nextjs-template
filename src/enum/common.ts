export enum MotionDirection {
  TOP = 'top',
  LEFT = 'left',
  RIGHT = 'right',
  BOTTOM = 'bottom',
}

export enum PageState {
  ENTER = 'enter',
  LEAVE = 'leave',
  IDLE = 'idle',
  PLAY = 'play',
}
